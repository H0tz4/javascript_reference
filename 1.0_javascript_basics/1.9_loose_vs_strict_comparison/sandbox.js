let age = 25;

// Loose comparison (different types cant still be equal)
console.log(age == 25);
console.log(age == '25');

// Strict comparison (different types cannot be equal.)
console.log(age === 25);
console.log(age === '25');